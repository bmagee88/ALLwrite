export function emotionMod(emotion: string) {
  return `the text i sent you above is a story or a part of a story, rewrite it in a ${emotion} way`;
}

// export function authorMod(author: string) {
//   return `the text i sent you above is a story or a part of a story, rewrite it in the style of ${author}`;
// }
