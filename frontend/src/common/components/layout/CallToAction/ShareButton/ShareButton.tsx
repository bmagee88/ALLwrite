import { Button } from "@mui/material";
import React from "react";

const ShareButton = () => {
  return (
    <Button
      variant='contained'
      size='small'>
      Share
    </Button>
  );
};

export default ShareButton;
